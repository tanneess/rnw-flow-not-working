{
  "name": "rnw-flow-not-working",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "flow-bin": "^0.65.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-native-web": "^0.5.1",
    "react-relay": "^1.5.0",
    "react-scripts": "1.1.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node ./setup && react-scripts start",
    "build": "node ./setup && react-scripts build",
    "test": "node ./setup && react-scripts test --env=jsdom",
    "eject": "node ./setup && react-scripts eject",
    "flow": "flow"
  }
}
